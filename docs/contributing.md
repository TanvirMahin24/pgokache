# Contributing

- Add hint and index rules under `backend/rules`.
- Include tests for normalization or API behavior where possible.
- Use PR template and include PG version + setup details for DB-related changes.
